apiVersion: v1
kind: Service
metadata:
  name: {{ include "orders-worker.fullname" . }}
  labels:
    {{- include "orders-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "orders-worker.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
