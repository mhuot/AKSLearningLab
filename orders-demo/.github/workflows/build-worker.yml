name: Build orders-worker

on:
  push:
    paths:
      - 'orders-demo/services/orders-worker/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Placeholder for orders-worker build"
